Useful commands (Darwin/macOS):
- `swift build` — build package
- `swift test` — run test suite
- `xcodebuild` commands if integrating in Xcode context
- `git status`, `git diff`, `git log` for VCS checks
- `ls`, `pwd`, `rg <pattern>` for local navigation/search
No dedicated formatter/linter command is defined in this repo metadata.